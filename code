# import the pyttsx module inside program
import pyttsx3

# initializing the module
e = pyttsx3.init()

# .say() function is used to speak the text you have written 
# inside the function
e.say("Anything you write here will be spoken by your computer during the running of the program")

# this is used to process and run the program commands
e.runAndWait()
